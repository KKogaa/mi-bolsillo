# Database Configuration
# Turso/LibSQL database URL
DATABASE_URL=libsql://your-database.turso.io
# Turso database authentication token
DATABASE_TOKEN=your-database-token

# Server Configuration
# Port for the HTTP server (Render will set this automatically)
# If not set, defaults to 8080
PORT=8080

# Email Provider Configuration (Optional)
# Email service API URL
EMAIL_PROVIDER_URL=https://your-email-provider.com/api
# Email service authentication token
EMAIL_PROVIDER_TOKEN=your-email-token

# Authentication Configuration
# Clerk JWKS URL for JWT verification
CLERK_JWKS_URL=https://your-app.clerk.accounts.dev/.well-known/jwks.json

# AI/ML Configuration
# Grok API key for intent detection
GROK_API_KEY=your-grok-api-key

# Telegram Bot Configuration
# Telegram Bot Token from @BotFather
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# OTP Configuration (Optional)
# OTP expiration time in minutes (default: 5)
OTP_EXPIRATION_MINUTES=5

# =============================================================================
# RENDER DEPLOYMENT INSTRUCTIONS
# =============================================================================
#
# 1. In Render Dashboard, go to your service > Environment
# 2. Add each of the above variables with your actual values
# 3. DO NOT commit the .env file to version control
# 4. Render will automatically inject these variables at runtime
# 5. The PORT variable is automatically set by Render - you can omit it
#
# For multiple services (echo server, telegram bot):
# - Each service needs its own set of environment variables in Render
# - Shared variables (DATABASE_URL, etc.) should be set for all services
# - Service-specific variables (TELEGRAM_BOT_TOKEN) only for relevant services
#
